import { TimeFormat } from './types';
type Props = {
    timeFormat: string;
    handleTimeFormatUpdate: (timeFormat: TimeFormat) => any;
};
declare const TimeFormatSelector: (props: Props) => import("react/jsx-runtime").JSX.Element;
export default TimeFormatSelector;
