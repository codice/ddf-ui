import { LazyQueryResults } from '../../../../js/model/LazyQueryResult/LazyQueryResults';
type Props = {
    lazyResults: LazyQueryResults;
    map: any;
    zoomToHome: (props: {
        map: any;
    }) => void;
};
declare const _default: ({ lazyResults, map, zoomToHome }: Props) => null;
export default _default;
