import { LazyQueryResults } from '../../../../js/model/LazyQueryResult/LazyQueryResults';
type Props = {
    lazyResults: LazyQueryResults;
    map: any;
};
declare const ZoomToSelection: ({ lazyResults, map }: Props) => null;
export default ZoomToSelection;
