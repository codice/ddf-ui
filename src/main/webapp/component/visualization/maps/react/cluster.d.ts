import { ClusterType } from './geometries';
type Props = {
    cluster: ClusterType;
    map: any;
};
declare const Cluster: ({ cluster, map }: Props) => import("react/jsx-runtime").JSX.Element;
export default Cluster;
