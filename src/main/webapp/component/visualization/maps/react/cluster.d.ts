/// <reference types="react" />
import { ClusterType } from './geometries';
type Props = {
    cluster: ClusterType;
    map: any;
};
declare const _default: ({ cluster, map }: Props) => JSX.Element;
export default _default;
