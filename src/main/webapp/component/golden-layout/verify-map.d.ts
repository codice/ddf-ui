/**
 *  Notice that we are only forwarding events that start with 'search' for now, as these are drawing events.
 */
export declare const useVerifyMapExistsWhenDrawing: ({ goldenLayout, isInitialized, }: {
    goldenLayout: any;
    isInitialized: boolean;
}) => void;
