import { FilterClass } from './filter.structure';
type Props = {
    filter: FilterClass;
    setFilter: (filter: FilterClass) => void;
};
declare const _default: ({ filter, setFilter }: Props) => JSX.Element;
export default _default;
